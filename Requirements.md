# On a fresh Debian trixie install

```sh
apt-get update && apt-get install -y \
  build-essential \
  clang \
  cmake-curses-gui \
  curl \
  git \
  gpg \
  libconfig-dev \
  libelf-dev \
  libgrpc++-dev \
  libgrpc-dev \
  libhyperscan-dev \
  libprotobuf-dev \
  procps \
  protobuf-compiler \
  protobuf-compiler-grpc \
  wget \
  lld
```

Installation of iouring wie im Repo angegeben
